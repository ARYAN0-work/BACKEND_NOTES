for stopping the terminal process -> cntrl + c

for unistall pkg

=> npm unistall pkgname

=> now theres nuclear approch => which will also delete npm modules but you can install it with nom insatll

=> the point if theres cache unistall node modules and redownload it